# Implementation Status

Live snapshot of design vs. reality, updated as code lands, not retroactively rewritten.
Companion to [`PROGRESS.md`](PROGRESS.md) (the "how we got here" log) and to `docs/ROADMAP.md`
(the phase plan). The design docs (`WHITEPAPER.md`, `docs/*`) describe intent; this table says
what actually exists in the repo right now, and where.

> **"Done" means implemented and unit-tested in this repo, it does not mean audited.** Per
> `docs/CRYPTOGRAPHY.md` §9 and `docs/GOVERNANCE.md` §3, no security claim ships to general
> availability without an independent external audit. Nothing here is a GA claim.

Legend: ✅ Done · 🚧 In progress · ⬜ Not started

---

## Phase 1: Core, Android-first

### Rust core (`mesh-core` crate, `core/`)

| Component | Design doc | Status | Code | Notes |
|---|---|---|---|---|
| Identity (Ed25519 + X25519, fingerprint, safety string) | `CRYPTOGRAPHY.md` §3 | ✅ | `core/src/identity.rs` | 7 tests (3 new: `to_bytes`/`from_bytes` roundtrip preserving fingerprint+signing, 64-byte determinism, different identities export different bytes). `to_bytes`/`from_bytes` added specifically to make native-layer identity persistence possible, also exported over UniFFI (`FfiIdentity::toBytes`/`fromBytes`, see the UniFFI bindings row), now consumed by `KeystoreIdentityStore.kt` on Android (see Native app table) so the fingerprint survives a restart instead of regenerating every launch |
| Noise `XX` handshake | `CRYPTOGRAPHY.md` §4.1 | ✅ | `core/src/crypto/noise.rs` | 1 test; interactive (both-in-range) case only |
| Noise → Double Ratchet handoff | `CRYPTOGRAPHY.md` §4.1/§5 | ✅ | `core/src/crypto/session.rs` | 2 tests; the glue that was missing, handshake transcript hash seeds the ratchet, responder's fresh ratchet pubkey delivered as one Noise-transport-encrypted message |
| Prekey async bootstrap (X3DH-style) | `CRYPTOGRAPHY.md` §4.2 | ✅ | `core/src/crypto/prekey.rs` | `SignedPrekey`/`OneTimePrekey`/`PrekeyBundle` + `initiate`/`respond` computing the 3-or-4-DH X3DH shared secret (HKDF-SHA256, 0xFF-padded per spec). Reuses the existing `DoubleRatchet::init_initiator`/`init_responder` API unchanged, Bob's signed prekey doubles as the initial ratchet public key, no ratchet code touched. **Prekey pool manager** (`PrekeyPool`): local bookkeeping tracking which one-time prekeys are available/handed-out/consumed, `needs_top_up`/`top_up`, and signed-prekey rotation, closes the gap this row flagged since it landed. **Explicitly doesn't solve** the distributed race where the same published bundle reaches two initiators before rotation (inherent to publish-to-many in a store-and-forward mesh, noted directly in `PrekeyPool`'s own doc comment). **Bundle transport decision made** (`docs/ROUTING-PROTOCOL.md` §5.1): a magic-byte-prefixed Broadcast envelope, same pattern as civic posts. `PrekeyBundle::to_bytes`/`from_bytes` wire format added as the concrete prerequisite. 16 tests total (11 new: 7 for the pool incl. a full bootstrap driven through it, 4 for the wire format incl. gossip-then-real-bootstrap). **`SignedPrekey`/`OneTimePrekey`/`PrekeyPool::to_bytes`/`from_bytes` added (later pass, same session)**, persistence for the pool's secrets, the prerequisite `HybridPrekeyPool::to_bytes`/`from_bytes` (see the PQXDH row) builds on. 4 new tests (20 total). **Now actually wired into a Kotlin messenger** (later pass, same session, see the PQXDH row for the hybrid FFI export this uses, and the Messaging UI row below for `DirectMessenger`'s `publishPrekeyBundle`/`initiateAsync`/`MAGIC_PREKEY_BUNDLE`): the plain classical path stays unexported at the FFI layer, deliberately, the app only ever bootstraps via the hybrid `HybridPrekeyPool`/`HybridBundle`, per `pqxdh.rs`'s own doc that hybrid "is the one a real app should actually call" |
| Double Ratchet (1:1) | `CRYPTOGRAPHY.md` §5 | ✅ | `core/src/crypto/ratchet.rs` | 6 tests: in-order, out-of-order, forward secrecy, post-compromise self-heal, tamper rejection |
| MLS groups (RFC 9420) | `CRYPTOGRAPHY.md` §6 | ✅ | `core/src/groups.rs` | **Not hand-rolled, uses the `openmls` crate** (pure-Rust `openmls_rust_crypto` backend, no C/OpenSSL toolchain risk), a deliberate choice given TreeKEM/epoch-state complexity is a different order of magnitude from this project's hand-rolled primitives (Noise, Double Ratchet), see the module doc comment for the full reasoning. Ciphersuite `MLS_128_DHKEMX25519_CHACHA20POLY1305_SHA256_Ed25519` (matches this project's other primitive choices). Group creation, add-member (KeyPackage/Commit/Welcome), joining via Welcome, and application messages all exercised through real wire serialization. **Durable persistence** (`snapshot_to_disk`/`load_group_from_disk`): rather than implementing `openmls_traits::storage::StorageProvider` from scratch (50+ methods, real complexity, deferred rather than rushed), treats `OpenMlsRustCrypto`'s in-memory `MemoryStorage` as an opaque `(key,value)` map and persists that wholesale (AEAD-sealed), reconstructing via `openmls`'s own `MlsGroup::load`. This module itself does **not** persist the member's own `SignatureKeyPair`, caller's job, same as the app master key (see the signer-export note below for how). **Routing integration** (`seal_as_envelope`/`open_from_envelope`): wraps MLS ciphertext as `Envelope`s using the `Addressing::Group` variant that already existed unused, selector = `BLAKE3(group_id)`; `RelayEngine` needs zero MLS awareness since it already treats every envelope's payload as opaque. 13 tests total in `groups.rs` (2 new: `add_member_from_bytes`, the FFI-facing parse-untrusted-bytes-then-validate wrapper, matching the typed path and rejecting garbage input, plus the original 9: simulated-restart survival with a real file, wrong-master-key rejection, envelope seal/open roundtrip, wrong-group rejection, 3-member scenario, tamper rejection). Release build reconfirmed. **Now exported over UniFFI** (`FfiMlsMember`/`FfiMlsGroupHandle`, see the UniFFI bindings row below): group creation, add-member, join-from-welcome, process-commit, seal/open, seal-as-envelope/open-from-envelope, and snapshot-to-disk/load-from-disk are all reachable from Kotlin now. 6 new FFI-layer tests (`ffi_groups.rs`), incl. a full two-member add→join→seal→open round trip and a seal-as-envelope→open-from-envelope round trip, purely through the FFI types. **Signer export added (later pass, same session):** `MlsMember::signer_to_bytes`/`signer_from_bytes` (via `tls_codec`, `SignatureKeyPair` already derives the needed traits) plus a new `MlsMember::from_identity_and_signer(identity, signer)` that rebuilds `CredentialWithKey` deterministically (`derive_credential_with_key`, it turned out to need no independent persistence at all, being a pure function of the app identity's fingerprint and the signer's public key, both already available). Exported over UniFFI as `FfiMlsMember::signerBytes`/`FfiMlsMember::fromIdentityAndSigner`. **`load_group_from_disk` can now actually resume signing as the group's pre-restart member**, the gap this row flagged since the MLS export pass. Proven with a real round trip (not just reusing in-memory objects across a fake "restart"): serialize the signer to bytes, drop every in-memory value, reconstruct purely from those bytes + the original `Identity`, then seal a new application message the group's other (never-restarted) member successfully opens. 2 new core tests (17 in `groups.rs` total). **Now wired into `GroupMessaging.kt`** (later pass, same session, see the Messaging UI row below for the full detail): every group's signer is Keystore-wrapped and every state-advancing call re-snapshots, restored on restart via `fromIdentityAndSigner`/`loadGroupFromDisk`. **Not done:** member removal/self-update/external-commit paths, small-group per-member-copy fallback (`CRYPTOGRAPHY.md` §6) unwired, snapshot cadence/atomicity not benchmarked at scale |
| Channels (Argon2id passphrase key) | `CRYPTOGRAPHY.md` §6 | ✅ | `core/src/crypto/channel.rs` | `Channel::from_passphrase` derives both the secret AEAD key and the public `Addressing::Channel` routing selector from one Argon2id call (via `crypto::passphrase`, shared with duress) + HKDF domain-separated expansion. **Unlike duress's random salt:** the Argon2id salt here is deterministic (`BLAKE3("MESH_CHANNEL_SALT" || passphrase)`), because two strangers who only share a spoken passphrase must independently derive the identical `(key, selector)` pair with no other coordination, a channel's entire trust model. `seal`/`open` wrap the existing many-use AEAD (`crypto::aead_seal`/`aead_open`), selector bound in as associated data. 6 tests incl. two independently-constructed `Channel`s from the same passphrase matching exactly, and a wrong-passphrase-guess failing to open. **Now exported over UniFFI** (`FfiChannel`, see the UniFFI bindings row below): `from_passphrase`/`selector_hex`/`seal`/`open`. Deliberately reuses `envelope_pack`'s existing `addressing_tag=1` path rather than adding a new pack/unpack function, `FfiChannel::seal`'s output is handed to `envelope_pack` as `sealed` with `addressing_target` set to `selector_hex`'s decoded bytes, exactly like every other addressing kind already works, so "no envelope-composition helper" from the previous pass turned out not to be a real gap once the FFI layer was actually built, mechanical composition on the Kotlin side, not a new Rust code path. 4 new FFI-layer tests (`ffi.rs`), incl. one exercising the full `seal` → `envelope_pack` → `envelope_unpack` → `open` round trip. **Now consumed by a real Kotlin UI too**, see the Messaging UI row below (`ChannelMessenger`), verified end to end on a real emulator, not just exported |
| PQXDH / post-quantum hybrid handshake | `CRYPTOGRAPHY.md` §6a | ✅ | `core/src/crypto/pqxdh.rs` | Builds on `crypto::prekey` rather than replacing it: `HybridBundle` = classical `PrekeyBundle` + a signed ML-KEM-1024 (FIPS 203) encapsulation key (new `PqPrekey`, via the `ml-kem` crate, RustCrypto, pure Rust, same "don't hand-roll a security-critical PQ construction" call already made for MLS/TreeKEM). `initiate`/`respond` run classical X3DH and an ML-KEM encapsulation/decapsulation side by side and combine both secrets in one HKDF (`classical_secret \|\| ml_kem_shared_secret`), secure if *either* half holds, per the doc's hybrid requirement. Real API verified via docs.rs/GitHub source before writing integration code (matching this project's openmls precedent), then confirmed via actual compiler errors for the exact method names (`generate_keypair_from_rng`, `encapsulate_with_rng`, `KeyExport::to_bytes`), several guesses from the docs summary were wrong and caught this way, not shipped. Needed a second RNG crate (`rand` 0.10, aligned with `ml-kem`'s `rand_core` 0.10 requirement) alongside the existing `rand_core` 0.6 used everywhere else in this crate, the two coexist fine since nothing needs to pass an RNG value across that boundary. 14 tests incl. a full hybrid-bootstrap-to-ratchet-messaging round trip and a tampered-PQ-signature rejection. Release build reconfirmed successful with the new dependency tree. **Real limit, documented in the module doc and matching `CRYPTOGRAPHY.md` §6a exactly:** stops a passive harvest-now-decrypt-later adversary; does not stop an active quantum attacker forging Ed25519 signatures, not a "quantum-proof" claim. **`HybridPrekeyPool` (new, later pass):** rotation/pool management `PqPrekey` didn't have before, composing `crypto::prekey::PrekeyPool`'s existing classical bookkeeping with a rotatable PQ prekey rather than duplicating it, the PQ half has no per-initiator-consumable analogue (ML-KEM encapsulation keys are reusable, unlike X25519 one-time prekeys), so this only adds what's actually different: the PQ keypair and its own `rotate_pq_prekey`. `HybridBundle::to_bytes`/`from_bytes` (wire format, mirrors `PrekeyBundle`'s) and `pack_initiation_message`/`unpack_initiation_message` (wire framing for the "first contact" reply Alice must send Bob, previously undecided, same kind of gap `docs/ROUTING-PROTOCOL.md` §5.1 closed for bundle transport) round out the pieces needed for a real async bootstrap. **Now exported over UniFFI** (`core/src/ffi_prekey.rs`, new module, mirrors `ffi_groups.rs`'s "mechanical wrapper" layering): `FfiHybridBundle` (parse/verify/`initiate`) and `FfiHybridPrekeyPool` (publish/rotate/`respond`), deliberately hybrid-only, not the plain classical bundle/pool, since `pqxdh.rs`'s own doc says the hybrid path "is the one a real app should actually call" and costs nothing extra. `initiate`/`respond` both return a ready-to-use `FfiSession` directly (via a new `FfiSession::from_ratchet` crate-internal constructor) rather than raw shared-secret bytes, so Kotlin never has to hand-construct a `DoubleRatchet`. **One-time-prekey secrets never cross the FFI boundary:** `respond` takes the pool and Alice's wire bytes, consumes the referenced one-time prekey internally, and hands back only the session, same boundary this crate already draws for MLS signing keys. 16 new tests total across `pqxdh.rs`/`identity.rs`/`ffi_prekey.rs` (171 core tests passing overall) (wire roundtrips, truncation rejection, replayed-one-time-prekey rejection, a full FFI-layer bootstrap-to-messaging round trip). Also added `PublicIdentity::to_bytes`/`from_bytes` (`identity.rs`) as a prerequisite, needed to carry Alice's identity in the initiation message. **Rebuilt into `jniLibs/` via `cargo ndk`** (all three ABIs, several times across this session) and reconfirmed `./gradlew assembleDebug` recompiles Kotlin against the new bindings each time. **`HybridPrekeyPool::to_bytes`/`from_bytes` added (later pass, same session)**, serializes the signed-prekey secret, one-time-prekey secrets, and PQ decapsulation-key seed (via `ml-kem`'s `DecapsulationKey::to_seed`/`from_seed`, verified real-round-trip: encapsulate against the restored key's public half, decapsulate with the restored private half, same shared secret). Exported as `FfiHybridPrekeyPool::toBytes`/`fromBytes`, Keystore-wrapped in `DirectMessaging.kt`, closes the "in-memory only" gap this row used to flag. 10 more new tests (Rust) + 2 FFI-layer. Now wired into `DirectMessenger` (see the Prekey row and Messaging UI row) |
| Metadata: link-identifier rotation | `CRYPTOGRAPHY.md` §7.1 | ✅ | `android/app/src/main/java/india/projectmesh/app/ble/BleTransportDriver.kt` | The advertised session ID (scan-response service data, used only for the dual-role connect tie-break) regenerates and re-advertises every 15 minutes (`SESSION_ID_ROTATION_INTERVAL_MS`), stop+restart advertising with fresh `AdvertiseData`, since BLE advertising payloads can't be updated in place. `@Volatile` + always-replace-the-array-reference (never mutate in place) so scan/GATT callbacks on Binder threads never observe a torn value. **Scope note, documented in the class doc:** this rotates the *application-layer* identifier this driver itself chose to broadcast, it does not and cannot control the underlying BLE MAC/link-layer address, which is Android's own platform privacy policy with no public API to force. Existing GATT connections are unaffected by rotation (the ID is only consulted pre-connection) |
| Metadata: envelope size bucketing | `CRYPTOGRAPHY.md` §7.2 | ✅ | `core/src/crypto/padding.rs` | `pad_to_bucket`/`unpad` operate on **plaintext before sealing**, not as an `Envelope` wire-format field, padding ends up inside the ciphertext, so the wire-visible effect (every envelope in a bucket has equal `sealed.len()`) falls out of AEAD's fixed overhead automatically, with zero changes to `envelope.rs`'s wire format or hop-stable ID logic. 6 buckets (64B .. 64KiB), a reasoned-not-benchmarked log-ish progression covering short text through voice-note-sized payloads; large media is expected on Wi-Fi Direct per `FEATURES.md` §6, not bucketed BLE envelopes. Oversized plaintext is rejected, not silently truncated. **Now wired into every per-message relay-visible seal path, not just Channels:** `Channel::seal`/`open` (`crypto/channel.rs`, first call site), `DoubleRatchet::encrypt`/`decrypt` (`crypto/ratchet.rs`, Direct messages), and `MlsGroupHandle::seal`/`open` (`groups.rs`, Group application messages, MLS's own epoch/sender framing rides on top unpadded, only the application-content length is normalized). 12 tests total (4 new: bucket-equality + boundary-crossing roundtrip for both ratchet and MLS group messages). Reachable from Kotlin automatically (no FFI signature changes needed on any of the three). **Deliberately not wired:** `aead_seal`/`aead_open`'s other two direct callers, `persistence.rs`'s at-rest envelope storage and `groups.rs`'s `snapshot_to_disk`, are local-disk-only, never relay-visible, so bucketing there would only waste disk space without closing any real traffic-analysis gap |
| Sphinx onion routing (optional) | `CRYPTOGRAPHY.md` §7.3 | ⬜ | | Phase 2 per roadmap |
| Encryption at rest | `CRYPTOGRAPHY.md` §8 | ✅ | `core/src/persistence.rs` | **deviation from doc, flagged:** `redb` (pure-Rust) + our ChaCha20Poly1305 AEAD, not literally SQLCipher, `rusqlite`'s SQLCipher backend needs OpenSSL compiled from source, which failed repeatedly on this project's Windows dev box even with MSVC + Strawberry Perl (see `PROGRESS.md`). Same security property (AEAD encryption at rest, tamper-evident), different engine. 4 tests + 2 FFI-layer tests. Revisit real SQLCipher if a working OpenSSL build environment (e.g. Android NDK's) becomes available |
| Duress / panic-wipe | `CRYPTOGRAPHY.md` §8 | ✅ | `core/src/persistence.rs`, `core/src/durable.rs`, `core/src/crypto/passphrase.rs` | `EncryptedStore::wipe`/`DurableStore::wipe`: best-effort secure delete (overwrite-then-remove) of the on-disk database file, consuming `self` first so the handle isn't held open (matters on Windows). No "duress mode" concept added anywhere, a decoy state is just an ordinary `DurableStore::open` at a different path with a key derived (via the new `crypto::passphrase` Argon2id module, shared with Channels) from a different passphrase; which passphrase maps to which (path, key) is native-layer/UX policy, not tracked here. 6 new tests (2 wipe, 4 passphrase-derivation). **Real limit, noted in the code:** overwrite-then-delete is not a guarantee against flash/SSD wear-leveling retaining old physical blocks, defense in depth on top of destroying the key itself (native-layer job, e.g. deleting an Android Keystore alias), not a standalone forensic guarantee, matching `THREAT-MODEL.md` §2 (A5)'s existing "mitigations, not guarantees" framing. **Not done:** no native-layer UI/flow for triggering panic-wipe or setting a duress passphrase yet |
| Envelope wire format | `ROUTING-PROTOCOL.md` | ✅ | `core/src/envelope.rs` | 7 tests; content-derived ID, not sender-trusted |
| Store-carry-forward engine (dedup, TTL, priority eviction) | `ROUTING-PROTOCOL.md` §7 | ✅ | `core/src/engine.rs` | 9 tests; in-memory index. `accept`/`purge_expired` now report the IDs that left the store, so `durable.rs` can mirror removals to disk |
| `Store` ↔ `EncryptedStore` integration (durability across restart) | `ARCHITECTURE.md` §5 | ✅ | `core/src/durable.rs` | `DurableStore`: accepted envelopes persist immediately; evictions and expiry-purges mirror to disk; reload on open repopulates the in-memory index and prunes anything that went stale while closed. 5 tests, incl. real close-and-reopen eviction/purge/reload checks |
| Bloom-filter summary vectors | `ROUTING-PROTOCOL.md` §7.1, §3 | ✅ | `core/src/bloom.rs` | Hand-rolled `BloomFilter` (double hashing off a single BLAKE3 digest, no new dependency), sized via the standard `m`/`k` formulas. `Store::summary_bloom`/`missing_from_bloom` and `relay.rs`'s `ContactMessage::Summary` now use it on the wire (`Store::summary_ids`/`missing_from` exact-set versions kept for callers that want an exact diff). 5 tests: no-false-negatives, empty-filter, empirical false-positive-rate bound, wire roundtrip, malformed-input rejection. **Not implemented:** the doc's "small explicit recent-ID list to bound false positives on hot items", a very-recently-composed envelope can, rarely, be skipped by a peer's false positive; self-heals on next contact, same best-effort semantics `ROUTING-PROTOCOL.md` §7 already states |
| Rate limiting | `ROUTING-PROTOCOL.md` §4.4 | ✅ | `core/src/relay.rs` | Per-peer fixed-window limiter (`RateLimitConfig`/`RateLimiter`), bucketed by `now / window_seconds` (deterministic, no wall-clock reads, consistent with the rest of this crate). Defaults (120 envelopes / 2,000,000 bytes per 60s per peer) are **reasoned, not benchmarked**, no target hardware available in this dev environment. Tunable via `RelayEngine::set_rate_limits` / `FfiMeshNode::set_rate_limits`. Over-budget frames are dropped silently (no `Err`, no store mutation, no relay), disconnect policy is left to the native transport layer |
| Client puzzle | `ROUTING-PROTOCOL.md` §4.5 | ✅ | `core/src/puzzle.rs` | Hashcash-style proof-of-work, `BLAKE3(envelope_id \|\| nonce)` with N required leading zero bits. **Deviation from doc, flagged:** bound to `envelope_id` only, not `(envelope_id, created_at)`, this implementation's `Envelope` has no `created_at` field (only `expires_at`); `envelope_id` already commits to the full envelope content, so this still achieves the doc's goal. Proof travels in `relay.rs`'s `ContactMessage::Data` (not inside `Envelope`'s own wire format) and is solved once by the originator, forwarded unchanged at every hop, cheaply re-verified at each. Difficulty `0` (default) disables it entirely, matching "(tunable/optional)". Default `DEFAULT_DIFFICULTY_BITS = 20` is reasoned, not benchmarked. 5 tests |
| Radio abstraction trait | `ARCHITECTURE.md` §3 | ✅ | `core/src/transport.rs` | Rust trait boundary only, no native implementation |
| Transport trait exposed over UniFFI | `ARCHITECTURE.md` §3, `TRANSPORT.md` | ✅ | `core/src/ffi_transport.rs` | `FfiMeshTransport` callback interface (Kotlin/Swift implements: start/stop/send, Rust calls it) + `FfiTransportHub` (native calls in: on_peer_discovered/on_peer_connected/on_frame/on_peer_lost, logged and drainable). 3 tests using a mock loopback transport |
| Contact protocol + epidemic relay ("the mesh engine loop") | `ROUTING-PROTOCOL.md` §1, §3, §4 | ✅ | `core/src/relay.rs` | `RelayEngine`: gossip-on-contact (Bloom-filter summary exchange, push-on-summary rather than the doc's explicit SUMMARY→WANT→DATA pull, flagged as a stated simplification, see the Bloom-filter row above), epidemic relay with TTL decrement (**now applied uniformly to both relay paths, see the DTN-sim-harness-found bug below**) and no-bounce-back-to-sender, plus rate limiting and the client puzzle (see their own rows above). Transport-agnostic, pure Rust, no hardware needed. 13 tests incl. 2-node and 3-node simulated-mesh scenarios. **Found and fixed a real bug while building this (original pass):** envelope IDs previously included `ttl_hops` in their content hash, so the same message got a different ID at every relay hop, silently defeating dedup beyond one hop. Fixed in `envelope.rs` (ID now excludes `ttl_hops`); regression test added. **Found and fixed a second real bug, this time via the DTN simulation harness (`dtn_sim.rs`, later pass, see its own row below):** the summary-triggered gossip push (`missing_from_bloom`-driven, the actual store-carry-forward path, as opposed to the live `relay_to_others`/`relay_to_all` flood path) never decremented `ttl_hops` at all. In the realistic case, every contact a separate, sequential pairwise window, no two peers ever simultaneously connected to trigger the live-relay decrement, hop count never bounded propagation; only `expires_at` did, silently narrower than "epidemic relay with TTL decrement" claimed above. Fixed: the gossip-push loop now calls `decrement_ttl` and skips forwarding once it returns `None`, exactly mirroring the live-relay path. 1 new regression test |
| Mesh engine wired to transport, over UniFFI | `ARCHITECTURE.md` §2 | ✅ | `core/src/ffi_node.rs` | `FfiMeshNode`: owns one `FfiMeshTransport` + a `RelayEngine`; `on_peer_connected`/`on_frame`/`compose_local` drive the protocol **and** call `transport.send(...)` automatically for whatever it produces, the native layer just reports events, all contact/relay/persistence logic lives here. `set_puzzle_difficulty`/`set_rate_limits` expose the tuning knobs above. `all_ids_hex`/`get_envelope_hex` (added for the messaging UI): the only way a native app can discover *new* inbound envelopes, since there's no arrival callback, poll, diff against what's already seen, fetch. 5 tests, incl. a full two-node gossip exchange through the complete FFI object graph (mock transport, no hardware) and the inbox-polling pair |
| **Real BLE/Wi-Fi/LoRa driver** (`android.bluetooth.*` etc.) | `TRANSPORT.md` §2 | 🚧 | | **Stale row, corrected:** this used to say "not written yet" from before the Android toolchain existed; both drivers have since been written and are tracked in detail in the Native app table below (BLE GATT driver row, Wi-Fi Direct/Aware driver row), see those rows for real status, don't duplicate here. LoRa has no driver of any kind (Phase 3/4, hardware not yet acquired) |
| UniFFI bindings | `ARCHITECTURE.md` §6 | ✅ | `core/src/ffi.rs`, `core/src/ffi_groups.rs`, `core/src/ffi_transport.rs`, `core/src/ffi_node.rs`, `core/src/ffi_prekey.rs` | identity, Noise handshake (`FfiHandshake`), ratchet session (`FfiSession`), passphrase channel (`FfiChannel`), MLS groups (`FfiMlsMember`/`FfiMlsGroupHandle`), hybrid X3DH/PQXDH async bootstrap (`FfiHybridBundle`/`FfiHybridPrekeyPool`, new, see the PQXDH row above), in-memory store (`FfiStore`), standalone durable store (`FfiEncryptedStore`), the two wired together (`FfiDurableStore`), transport callback interface (`FfiMeshTransport`/`FfiTransportHub`), and the fully-wired node (`FfiMeshNode`, use this one in a real app) all exported + tested (37 FFI-layer tests: 16 in `ffi.rs`, 8 in `ffi_groups.rs`, 3 in `ffi_transport.rs`, 4 in `ffi_node.rs`, 6 in `ffi_prekey.rs`); Kotlin bindings generated (`core/generated/`, copied to `android/`) and the native `.so` rebuilt via `cargo ndk` for all three ABIs, `./gradlew assembleDebug` reconfirmed against the new surface (four times across this session: PQXDH export, MLS signer export, prekey pool persistence, and the padding-wiring pass). 191 core tests passing overall. Not exported yet: onion routing (Phase 2), plain classical (non-hybrid) prekey bundles (deliberately, see the PQXDH row) |

### Native app

| Component | Design doc | Status | Code | Notes |
|---|---|---|---|---|
| Android app shell (Gradle/Kotlin/Compose) | `ARCHITECTURE.md` §2 | ✅ | `android/` | `./gradlew assembleDebug` **succeeds**, real signed debug APK (~20 MB) built, native `mesh-core` bundled. Full toolchain now present: Android SDK + NDK r27c + cmdline-tools installed, Gradle 8.11.1 + real wrapper generated (replacing the earlier properties-only stub). Fixed along the way: Kotlin 2.0+ needs the separate Compose Compiler Gradle plugin; `local.properties` `sdk.dir` needed forward slashes (backslash-escaping broke Java properties parsing); `AndroidManifest.xml` had `--` inside an XML comment (invalid XML); `Theme.Material3.DayNight.NoActionBar` needed the classic `com.google.android.material:material` dependency (Compose Material3 alone doesn't provide XML theme resources). **Now verified on a real emulator** (AVD `mesh_test`, Android 15 / API 35, `google_apis` x86_64 image, headless): app installs, launches without crash, and tapping "Generate identity" produces a real fingerprint + safety string, confirming the full round trip Kotlin → JNA → `libmesh_core.so` → Rust `FfiIdentity::generate()` → back to the UI. Not yet tested: a physical device, or anything beyond this one identity-generation screen |
| `mesh-core` cross-compiled for Android (`.so`) | `ARCHITECTURE.md` §6 | ✅ | `android/app/src/main/jniLibs/` | Built via `cargo-ndk` against NDK r27c for arm64-v8a, armeabi-v7a, x86_64 (confirmed real ELF binaries, e.g. arm64 `.so` is "ELF 64-bit LSB shared object, ARM aarch64 ... for Android 21, built by NDK r27c"). `i686-linux-android` target installed but not built into `jniLibs/` by default |
| BLE GATT driver | `TRANSPORT.md` §2 | 🚧 | `android/app/src/main/java/india/projectmesh/app/ble/` | Real `android.bluetooth.*` dual-role driver: `BluetoothGattServer` (peripheral: advertise + serve) and per-peer `BluetoothGatt` (central: scan + connect), wired to `FfiMeshNode` via `MultiTransport` (not `FfiTransportHub`, that object only logs events, see `ffi_transport.rs`'s doc comment; see the `MultiTransport` row below for why it's no longer the sole transport). Two characteristics (one per direction) rather than the doc's implied single characteristic, GATT's data model is inherently asymmetric. 4-byte fragmentation header (message_id/flags/fragment_index) chunks frames to the negotiated ATT MTU (requests 517, falls back to 23). Dual-role connect race (both sides advertise+scan, could double-connect) resolved by a random session-ID tie-break in scan-response service data, since `BluetoothAdapter.getAddress()` can't read a device's own real MAC on modern Android. **Verified:** builds clean, installs, launches without crash, "Start mesh" starts advertising successfully on a real emulator (AVD `mesh_test`, logcat confirms `advertising started`). **Not verified:** an actual two-device BLE exchange (scan discovery, GATT connect, frame exchange), attempted with a second emulator (`mesh_test2`) but this dev host cannot sustain two headless Android emulators simultaneously (steady 100% CPU, `system_server` ANRs on the second instance even freshly booted, not a stuck-state issue, confirmed by killing and recreating it, which reproduced the same ANR immediately). Real cross-device verification needs either a beefier host or physical hardware. **Deliberate simplifications, not yet addressed:** connections kept open while peers stay in range instead of `TRANSPORT.md` §2.2's short-lived connect/exchange/disconnect cycle (battery cost unaddressed); no backoff/fairness for dense crowds, hardcoded `MAX_CONNECTIONS = 4`; runs inside a foreground service now (see the row below) but that service still has no recovery UX if the OS kills it anyway; no BLE-adapter-disabled recovery flow; master key is now Android-Keystore-backed, see the dedicated row below, no longer an outstanding item |
| Android Keystore-backed master key + identity/session persistence | `CRYPTOGRAPHY.md` §8 | ✅ | `android/app/src/main/java/india/projectmesh/app/KeystoreSecretBox.kt`, `KeystoreMasterKey.kt`, `KeystoreIdentityStore.kt` | **`KeystoreSecretBox.kt` (new, later pass):** the master-key wrap/unwrap logic extracted into a generic Keystore-backed encrypt/decrypt-by-alias helper once a second real caller (identity persistence) needed the identical pattern, `KeystoreMasterKey.kt` now delegates to it. Same helper backs `ChannelMessaging.kt`'s joined-passphrase persistence. Key-wrapping design: `FfiMeshNode.open`'s 32-byte master key is still an ordinary `SecureRandom`-generated value (Rust has no notion of Android Keystore, so it needs real extractable bytes), but is now **encrypted** under a hardware-backed (TEE/StrongBox, device-dependent) non-extractable AES-256-GCM key generated in `AndroidKeyStore` before being persisted, the wrapped ciphertext is what actually sits in `SharedPreferences` now, not the plaintext key. **One-time migration:** any leftover plaintext key from the pre-Keystore scheme (a real, live secret already sitting in the same prefs file for any device that ran the old code) is explicitly wiped on next load, not just silently superseded. `setUserAuthenticationRequired(false)`, usable without a biometric/lock-screen prompt since `MeshRelayService` needs to open the store automatically in the background; protects against offline extraction (root, ADB backup) not a compromised-while-unlocked device, same threat-model boundary `docs/THREAT-MODEL.md` already draws elsewhere. **Verified on a real emulator, not just compiled:** confirmed via `run-as`+`cat` on the actual `SharedPreferences` XML file (not just reading the code) that the wrapped ciphertext is what's stored, that it's byte-for-byte identical across an app reinstall (proving both the Keystore key and the wrapped blob genuinely persist, not regenerated each launch, GCM never produces identical ciphertext from a fresh encrypt), and that the migration-cleanup log line fires and the legacy plaintext entry is actually gone afterward. **Real bug found and fixed along the way:** `DurableStore::open` (`core/src/durable.rs`) used to propagate a single envelope's AEAD decrypt failure as a fatal `Err`, aborting the *entire* store open, reachable in practice the moment a master key can legitimately change (exactly what this Keystore work does the first time it runs on a device with pre-existing data, and what a future Keystore reset would do again). Fixed to drop just the undecryptable envelope and continue, matching this crate's existing best-effort mesh semantics elsewhere; 1 new regression test proves `open()` now succeeds instead of erroring when reloading under a different key. 133 core tests passing (1 new) |
| Foreground service / OEM battery-whitelist UX | `TRANSPORT.md` §6 | ✅ | `android/app/src/main/java/india/projectmesh/app/MeshRelayService.kt`, `OemBatteryGuidance.kt` | `MeshRelayService`: thin foreground service (`FOREGROUND_SERVICE_TYPE_CONNECTED_DEVICE`) owning no mesh logic itself, starts/stops the shared `MeshCoordinator`, ties the mesh's lifetime to the service's rather than the Activity's. `MainActivity`'s "Start mesh" now starts this service (`ContextCompat.startForegroundService`) instead of calling the coordinator directly; `MeshScreen`'s UI state polls `coordinator.isRunning()` rather than tracking a locally-set flag, so it stays honest if the service is started/stopped/restarted independently of the composable. `OemBatteryGuidance`: standard `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` exemption request (generic, always available) plus known vendor-specific autostart/protected-apps settings screens for Xiaomi/Oppo/Vivo/realme/Samsung/OnePlus, **reasoned from public community-collected component names, not verified against every OEM/OS-version combination**, and that gap is disclosed here rather than glossed over. **Verified on a real emulator, full lifecycle:** start → mesh runs inside the service (`isForeground=true`, correct service type `0x10`) → app backgrounded (HOME) → service, notification (`ONGOING_EVENT\|NO_CLEAR\|FOREGROUND_SERVICE`), and BLE advertising all confirmed still alive → app foregrounded again → Stop mesh → clean teardown (`onDestroy` logged, no crash, no leftover foreground state). Confirmed the service correctly rejects external `am stopservice` calls (`exported=false` doing its job). **Not done:** custom notification icon (uses a placeholder platform drawable), no in-app UX for what happens if the OS still kills the process despite the foreground service (STICKY restart is relied on, not independently tested under real memory pressure) |
| Wi-Fi Direct / Aware driver | `TRANSPORT.md` §4 | 🚧 | `android/app/src/main/java/india/projectmesh/app/wifidirect/` | Real `android.net.wifi.p2p.*` implementation of `FfiMeshTransport`: DNS-SD service discovery (TXT record carries the mesh `serviceId`, so only matching-mesh peers are connected to, not every nearby Wi-Fi Direct device) for rendezvous, then a plain length-prefixed TCP socket per connected peer once a P2P group forms (group owner runs a `ServerSocket`, the other side connects out to `groupOwnerAddress`). Same driver contract and connection-race tie-break pattern as `BleTransportDriver` (lower `deviceAddress` initiates `connect()`), but peer identity here is IP-based rather than device-address-based, a stated simplification, correlating an accepted socket back to a specific `WifiP2pDevice` needs a second `requestGroupInfo` round trip not done this pass. **Now wired into `MeshCoordinator`** via `MultiTransport` (see its own row below), runs alongside BLE, not standalone anymore. `MainActivity`'s runtime permission request extended to also cover Wi-Fi Direct's own grant (`ACCESS_FINE_LOCATION` on API 29-32, `NEARBY_WIFI_DEVICES` on 33+, independent of what BLE's permission model needs at the same API levels). **Compiles clean (`./gradlew assembleDebug` succeeds), not run against real hardware.** This dev environment has no physical Android device and Wi-Fi Direct has no meaningful emulator support (no virtual P2P radio), so this is unverified beyond "it builds," same honest scope BLE's driver had before a device existed. **Not done:** no BLE-bootstrapped discovery (`TRANSPORT.md` §4's suggested pattern), this driver's DNS-SD discovery runs independently. No short-lived connect/exchange/disconnect cycle, no backoff/fairness, no foreground-service-specific integration beyond what `MultiTransport` inherits from `MeshCoordinator` already running inside `MeshRelayService`, no Wi-Fi-disabled recovery flow. |
| `MultiTransport` (BLE + Wi-Fi Direct composed behind one `FfiMeshTransport`) | `ARCHITECTURE.md` §2, `TRANSPORT.md` §1 | 🚧 | `android/app/src/main/java/india/projectmesh/app/MultiTransport.kt` | `FfiMeshNode` takes exactly one transport at construction; this class holds both drivers, sets itself as each one's `MeshEventSink`, and remaps their independently-numbered local peer handles (`BlePeerRegistry`/`WifiPeerRegistry` each start at 1) into one global handle space, the only way to avoid handle collisions between the two links. `start()` starts BLE unconditionally and Wi-Fi Direct best-effort (a device with no Wi-Fi Direct hardware/support logs a warning and keeps running on BLE only, rather than failing the whole mesh). `MeshCoordinator` now constructs `MultiTransport(BleTransportDriver, WifiDirectTransportDriver)` instead of holding a bare `BleTransportDriver`. **Verified:** compiles clean, `MeshCoordinator`/`MainActivity` build against the new shape with no other call-site changes needed (`coordinator.isRunning()`/`connectedPeerCount()` signatures unchanged). **Not done:** no real two-link test (blocked on the same no-hardware/no-emulator gap both underlying drivers have); a peer reachable over both links simultaneously is represented as two distinct peer handles today, not deduplicated at the peer-identity level (harmless, `RelayEngine`'s envelope-ID dedup already absorbs the double delivery, but not bandwidth-optimal, stated in the class doc comment). |
| SOS | `FEATURES.md` §1 | ✅ | `android/app/src/main/java/india/projectmesh/app/messaging/SosMessaging.kt` | One-tap `Priority::Sos` broadcast (category: Medical/Trapped/Fire/Violence/Other), `RelayEngine`'s existing priority-ordering/eviction rules already give it top priority, zero core changes needed. **Acknowledgement is itself a public broadcast**, not a private reply to the sender, deliberate: `Addressing::Broadcast` carries no sender identity (unlike Direct, which embeds one for handshake routing), and adding one just for SOS would deanonymize whoever's in danger. Shares `CivicPost.kt`'s wire framing with bulletins/resource-board. **Verified on a real emulator:** sent an SOS with a chosen category, confirmed it appeared correctly tagged; tapped Acknowledge and confirmed the UI flipped from "Not yet acknowledged" to "Acknowledged" via a real compose→store→poll→decode round trip, not a local-only flag. **Not done:** no device location (`FEATURES.md`'s "optional coarse location", needs a location permission + `FusedLocationProvider` flow, not attempted), no distance/direction display, unsigned |
| Disaster bulletin board | `FEATURES.md` §2 | ✅ | `android/app/src/main/java/india/projectmesh/app/messaging/BulletinMessaging.kt` | `Priority::Bulletin` broadcast, category (relief camp/food/water/medicine/road status/shelter/missing person/other), TTL via the envelope's own `expires_at`/`ttl_hops`. Verified on a real emulator: posted and watched it round-trip through the full compose→store→poll→decode→display pipe. **Not done:** no responder-key endorsement (`FEATURES.md`'s "known responder keys visually distinguished" needs a trusted-key list this app doesn't have), unsigned |
| Offline maps (MapLibre) | `FEATURES.md` §3 | 🚧 | `android/app/src/main/java/india/projectmesh/app/MapScreen.kt` | MapLibre Native Android SDK (`org.maplibre.gl:android-sdk:11.13.0`) integrated and rendering, a genuinely offline (zero-network-call) minimal style loaded via `Style.Builder().fromJson(...)`, not a remote style URL, matching this app's deliberate absence of the `INTERNET` permission. **Verified on a real emulator, not just compiled:** installed, launched, no crash; scrolled to the map section and confirmed the style's background color actually renders (a real GL surface came up, not a placeholder). **Not done, to be clear: this is the rendering pipeline, not offline maps as `FEATURES.md` §3 describes.** no OpenStreetMap vector tiles, no MBTiles/PMTiles regional tile-pack loading, no tile-pack sideloading or Wi-Fi-Direct-based pack sharing (needs real geographic tile data this dev environment has no way to source), no pin-dropping-over-the-mesh protocol. The SDK-integration/proof-of-offline-rendering step is done; the actual map-data work is a separate, larger follow-up |
| Resource board | `FEATURES.md` §4 | ✅ | `android/app/src/main/java/india/projectmesh/app/messaging/ResourceMessaging.kt` | Have/Need + category (food/shelter/transport/tools/blood donor/charging/labour/other), `Priority::Normal` (the `Priority` enum has no dedicated resource-board tier, a known coarseness, not fixed this pass). Same shared wire framing and verification method as bulletins. **Not done:** no have/need matching or search (flat feed only), unsigned |
| Messaging UI (Direct + Broadcast + Channel + Group) | `FEATURES.md` | 🚧 | `android/app/src/main/java/india/projectmesh/app/messaging/` | All four addressing modes now have a real screen. `DirectMessenger`: Noise XX handshake -> Double Ratchet, carried as `Addressing::Direct` envelopes. App-layer wire framing (`[msg_type:1][sender_fingerprint:32][payload]`) prepends the sender's fingerprint since Noise XX deliberately hides identity until partway through the handshake and `Addressing::Direct`'s target is the recipient, not sender, without it a receiver couldn't route an incoming handshake message to the right contact. Dual-initiate race resolved by the same lower-fingerprint-initiates tie-break pattern already used for the BLE connect race. Receiving is polling-based (`all_ids_hex`/`get_envelope_hex` on `ffi_node.rs`). `BroadcastMessenger`: plain UTF-8 posts, **unsigned**. `ChannelMessenger`: passphrase-derived joined channels via `FfiChannel`, multi-session shape mirroring `DirectMessenger`'s contact list. `GroupMessenger` (new): MLS groups via `FfiMlsMember`/`FfiMlsGroupHandle`. **Deliberately manual/out-of-band key exchange**, same simplification `DirectMessenger` already uses for fingerprints: a `KeyPackage` is shared as hex text for someone to paste into "Add member," the resulting Commit and Welcome come back as hex text to distribute the same way. **Real gap, not glossed over:** Commit distribution to every *other* existing member (so they stay in sync with a new epoch) has no automated mesh transport yet, only application-message traffic (`seal_as_envelope`/`open_from_envelope`, `Addressing::Group`) is automatic, same as Channel. `MainActivity`'s identity screen displays one stable `MeshApplication`-owned session identity. **Verified on a real emulator, not just compiled:** posted a broadcast and watched it round-trip through compose→store→poll→decode→display; added a Direct contact and confirmed the tie-break correctly deferred; joined a Channel by passphrase, confirmed session persistence through an accidental activity backgrounding, posted and watched the round trip; created an MLS group ("relief-team"), generated a real ~612-byte `KeyPackage` for a second member identity, and successfully called `addMember` with it, produced real Commit (~1500+ byte) and Welcome (~850-byte) MLS wire messages, both rendered correctly in the UI, no crash, no exception logged. **Not independently verified on-device:** the `joinFromWelcome` success path specifically, blocked by ADB's `input text`/keyevent tooling being unreliable for pasting/editing 1,600+ character strings into a multi-line Compose field (extensively attempted; typed content repeatedly got corrupted by the automation tool itself, confirmed via exact diffs against the expected hex), not by any app-level error. This exact path (`joinFromWelcome` consuming a pending member and producing a live `FfiMlsGroupHandle`) is proven correct at the Rust/FFI layer by `ffi_groups.rs`'s `two_member_roundtrip_through_the_ffi_layer` test, which calls the identical generated binding. **Identity, Direct contacts, and joined Channels now persist across restarts** (later pass, same session, see `KeystoreIdentityStore.kt`/`DirectMessaging.kt`/`ChannelMessaging.kt`): the app's identity is Keystore-wrapped and reloaded via `FfiIdentity::toBytes`/`fromBytes` (new this pass), Direct contacts' fingerprints are saved to plain `SharedPreferences` (public values, no encryption needed), and joined Channels' passphrases are Keystore-wrapped and re-derived via `FfiChannel::fromPassphrase` on load. **Verified on a real emulator across a full force-stop + relaunch cycle:** identity fingerprint identical before/after; a joined channel (`supply-drop-9`) reappeared with the same selector; an added Direct contact reappeared at `NO_SESSION`/`HANDSHAKING` as expected. **Message/session history still does not persist**, only *that* you'd joined a channel or added a contact, not the conversation itself; a restored Direct contact needs a fresh Noise handshake next contact window (the live ratchet session isn't persisted, forward-secret key material, a separate and larger design question, not attempted this pass). **Group persistence now wired** (later pass, same session, see the MLS signer-export row above): each `createGroup`/`joinFromWelcome` call's distinct MLS signer is Keystore-wrapped (`GroupMessaging.kt`, `FfiMlsMember::signerBytes`), and every state-advancing call (creation, `addMember`, `processCommit`, `send`, receiving a post) re-snapshots the group to an AEAD-sealed file keyed by the app's shared master key. Restored in `GroupMessenger.init` via `FfiMlsMember::fromIdentityAndSigner` + `FfiMlsGroupHandle::loadGroupFromDisk`; a group that fails to restore is logged and skipped, not fatal to the others. **Verified on a real emulator, not just compiled:** created a real group ("qa-relief-team") on a fresh install, confirmed the Keystore-wrapped signer (`mesh_groups.xml`) and AEAD-sealed snapshot (`mesh_group_snapshot_<id>.bin`, 5,676 bytes) were actually written to app-private storage, then sent an application message and confirmed the snapshot file grew (5,676→5,886 bytes), proving the state-advance-then-resnapshot path really executes, not just compiles. **Async (offline-first-contact) bootstrap wired into `DirectMessenger`** (later pass, same session, see the PQXDH/prekey rows above for the FFI it uses): each `DirectMessenger` owns an in-memory `FfiHybridPrekeyPool`, publishes its current bundle as a `MAGIC_PREKEY_BUNDLE`-prefixed Broadcast envelope (`publishPrekeyBundle`, called once at construction), and caches other devices' bundles it sees broadcast (`knownBundles`/`knownBundleFingerprints`). `initiateAsync(contact)` bootstraps a session from a cached bundle instead of the interactive Noise handshake, works even if the contact isn't reachable right now, and sends the resulting "first contact" message (new `MSG_TYPE_ASYNC_INIT` direct-frame type) through the ordinary store-and-forward mesh; `handleAsyncInitFrame` completes the session on the receiving side whenever it's next polled, no live round trip needed. Deliberately opt-in per contact (a new "message now via their prekey bundle" button, shown only once `hasKnownBundle` is true and the contact isn't `CONNECTED` yet), not auto-fired alongside the interactive attempt, to avoid two bootstrap mechanisms racing for the same contact. **Prekey pool now persists too** (later pass, same session): `HybridPrekeyPool::to_bytes`/`from_bytes` (Rust, new, serializes the signed-prekey secret, one-time-prekey secrets, and PQ decapsulation-key seed) exported as `FfiHybridPrekeyPool::toBytes`/`fromBytes`, Keystore-wrapped in `DirectMessaging.kt` and re-persisted after every mutating call (`respond` consumes a one-time prekey). A bundle published in a previous app session can now actually be answered after a restart. **Verified on a real emulator:** confirmed `mesh_prekey_pool.xml` (Keystore-wrapped) is written on first launch. **Found and fixed a real bug via this same on-device pass, not a hypothetical:** the prekey bundle's `MAGIC_PREKEY_BUNDLE` (`0xF4`) byte was missing from `BroadcastMessenger`'s exclusion filter (`isCivicMagic`, which only knew about SOS/bulletin/resource `0xF1`-`0xF3`), so on every launch, the real ML-KEM-1024/X25519 bundle bytes leaked into the plain-chat Broadcast feed as garbled binary noise (Kotlin's lossy UTF-8 decoder replacing invalid bytes with `�`), reproduced identically across a full app uninstall and even a complete emulator data wipe (ruling out stale state, it re-publishes fresh on every launch via `DirectMessenger.init`). Fixed by moving `MAGIC_PREKEY_BUNDLE` into `CivicPost.kt`'s shared magic-byte registry and renaming the check to `isReservedBroadcastMagic` so the exclusion list can't miss a reserved magic byte again the same way. Verified clean on a fresh install after the fix. **Not done:** no two-device handshake/message-exchange test for any mode (same host-capacity limit as the BLE driver); no QR-code trust establishment for any mode; no leave/forget-channel or leave-group action; no member-removal/self-update UI for groups |
| Localization foundation | `LOCALIZATION-UX.md` §1 | 🚧 | `android/app/src/main/res/values/`, `values-bn/` | **English-only for now; Bengali paused, not deleted** (per-user decision, 2026-07-26 second pass, see `PROGRESS.md`): `values-bn/strings.xml` is untouched and still has an **identical key set** to `values/strings.xml` (`diff` of sorted key lists, zero difference), but both of the app's language pickers (`LanguagePickerScreen.kt`, `LanguageScreen.kt`) carry the Bengali row commented out rather than removed, and `android/app/build.gradle.kts` sets `defaultConfig.resourceConfigurations += listOf("en")` so Bengali is not actually packaged into the APK even though its source strings remain. Re-enabling is uncommenting two rows and removing that one Gradle block, not redoing translation work. Every UI string is still referenced via `stringResource()`/`@StringRes`, unaffected by this. **Previously verified (before the pause) on a real emulator:** the app's language switched live via Android's per-app language API and every on-screen string rendered correctly in Bengali with correct conjuncts and no mojibake/missing-glyph boxes. **Not done:** every other language (Hindi, Assamese, Bodo, etc.) stays community-contributed per the doc's own model, not shipped by default; Noto font bundling, complex-script shaping guarantees independent of system fonts, RTL, TTS (see the Betar UI redesign row below for what TTS work exists), voice notes |
| F-Droid / reproducible build pipeline | `DISTRIBUTION.md` | 🚧 | `metadata/india.projectmesh.app.yml`, `docs/REPRODUCIBLE-BUILD.md` | Toolchain versions pinned and documented (Rust 1.96.0, `cargo-ndk` 4.1.2, NDK r27c, AGP 8.7.2, Kotlin 2.0.21, Gradle 8.11.1, all already-exact-pinned Android dependency versions confirmed, no `+` ranges). **Same-machine reproducibility actually verified, not assumed:** the `arm64-v8a` `.so` built twice from a clean target dir, identical source, produced byte-for-byte identical SHA-256 hashes. F-Droid build-recipe metadata added, reasoned from the public `fdroiddata` schema. `LICENSE` file added (AGPL-3.0-or-later, matching the already-declared license). **Release signing now configured** (later pass): a real `CN=Betar` RSA-4096 keystore, `./gradlew assembleRelease` produces a genuinely signed APK, verified via `apksigner verify --print-certs`, not the debug cert. Key lives only on this machine, gitignored, no backup or rotation plan yet, see `REPRODUCIBLE-BUILD.md` §4. **Not done:** metadata not validated against F-Droid's own linter/build server (no real submission attempted this session, deferred to a later pass, not this one); cross-machine/cross-OS reproducibility untested (only one machine available here); full-APK (not just the `.so`) reproducibility untested |
| DTN simulation harness | `ARCHITECTURE.md` §7 | ✅ | `core/src/dtn_sim.rs` | `Simulation`: drives real `RelayEngine`/`DurableStore` instances (no mock transport, no reimplemented protocol, the same `on_peer_connected`/`on_frame`/`compose_local`/`on_peer_lost` API a real BLE/Wi-Fi driver calls) through a caller-scripted sequence of pairwise contact windows (`(tick, node_a, node_b)` triples), measuring `delivery_ratio` under partition/churn. **What it simulates and what it doesn't:** contact *timing* is entirely caller-scripted input, no mobility model, physical layer, or radio propagation of its own (that's the separate, much larger LoRa/hardware phase work, not attempted here). What it does exercise for real: the actual gossip protocol, TTL, rate limiting, and persistence, node-for-node. 5 tests: linear multi-hop chain propagation across sequential (never simultaneous) contact windows, an irrelevant early contact causing no harm, partition-then-heal via a single bridge contact, TTL exhaustion partway down a long chain, and no-path non-delivery. **Found and fixed a real bug while building this**, see the Contact-protocol row above for the gossip-push TTL-decrement fix it surfaced |
| Betar UI redesign (onboarding, 5-tab shell, emergency flow) | `DESIGN-BRIEF.md` §9 | 🚧 | `android/app/src/main/java/india/projectmesh/app/ui/` | Real screens, not the old single-scrolling-column debug skeleton: onboarding (language picker, intro pager, nickname, permissions explainer, battery guidance), `BetarScaffold` (mesh ribbon, 5-tab bottom nav: Chats/Nearby/Board/Map/You, persistent SOS button), and the full emergency flow (category pick, detail, slide-to-send, live status). Chats/Board/Emergency wire to the real `DirectMessenger`/`ChannelMessenger`/`GroupMessenger`/`SosMessenger`/`BulletinMessenger`/`ResourceMessenger` already documented in the Messaging UI row above, not a new mock layer. **Verified on a real emulator, full walkthrough, not just compiled:** fresh install through the entire onboarding chain including the real system permission dialogs (granted), reaching the main shell, opening the emergency picker and confirming its close button actually dismisses. **Two real bugs found and fixed via that same walkthrough:** the mesh ribbon and the emergency flow's header both rendered underneath the system status bar (missing `.statusBarsPadding()`), making the emergency close button's tap target unreachable until fixed. **Real gaps, not glossed over:** no per-device peer list backend yet (Nearby's device-list UI is built but has no data source), no pin-over-mesh transport for the Map tab's dropped pins, no QR/camera library wired (scan-to-add and identity QR are stubbed), no audio capture backend for voice notes, hero illustrations are placeholder blocks (no asset/illustration pipeline). Launcher icon is the real adaptive Betar mark now (inverted per `DESIGN-BRIEF.md` §7: blue background layer, off-white wire-bar foreground), replacing the old tricolor glyph; the Android package id and Kotlin package name are the one deliberately-still-unchanged piece, see `BETAR-TRANSITION.md` Part 5 |
| Wire-parser fuzzing | `ARCHITECTURE.md` §7 | 🚧 | `core/fuzz/` | 6 `cargo-fuzz` targets covering every untrusted-bytes-in parser reachable from the mesh before any signature check runs: `Envelope::from_bytes`, `BloomFilter::from_bytes`, `ContactMessage::from_bytes`, `PrekeyBundle::from_bytes`, `HybridBundle::from_bytes`, `unpack_initiation_message`. Source verified via a plain `cargo check` (compiles cleanly, no instrumentation). **Not actually run in this dev environment (see `core/fuzz/README.md` for the full detail):** `cargo fuzz run` fails to *build* on this Windows/MSVC box, not from a fuzzing-found bug but a linker conflict, `mesh-core`'s `cdylib` crate-type (needed for Android) gets built as a side effect of the fuzz binary's dependency, and MSVC's linker rejects that spurious `cdylib` output for missing sanitizer/coverage runtime symbols the real fuzz binary would normally provide. Confirmed via the actual linker errors (tried both `-s address` and `-s none`), not assumed; a Windows/MSVC-specific issue, not expected on Linux. Needs a Linux/macOS machine or CI runner to actually execute, not attempted this session (no CI exists yet for this repo) |

## Phase 2: Reach and hardening

| Component | Status |
|---|---|
| iOS app (Swift/SwiftUI, CoreBluetooth, MultipeerConnectivity) | ⬜ not started |
| Android ↔ iOS BLE bridging | ⬜ not started |
| Independent external security audit | ⬜ not started (hard release gate, see `GOVERNANCE.md` §3) |

## Phase 3 / 4: LoRa backbone, ecosystem

Not started; see `docs/ROADMAP.md` and `docs/HARDWARE-LORA.md`. No hardware work has begun.

---

## How to update this file

When a component's status changes, edit its row here in the same commit as the code change, and
add a dated entry to `PROGRESS.md` describing what changed and why. Don't let this file drift
from the code, a stale status table is worse than none.
