<?xml version="1.0" encoding="utf-8"?>
<!-- Android 13+ themed-icon layer (Material You icon theming): the system applies its own
     single tint to this drawable, so it carries only the wire-bar silhouette, same geometry as
     ic_launcher_foreground.xml, filled rather than tied to the brand's own colours since the
     system overrides them anyway. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#00000000"
        android:strokeColor="#000000"
        android:strokeWidth="14"
        android:strokeLineCap="round"
        android:pathData="M27,54 L39,54 M69,54 L81,54" />
</vector>
